name: Build Android APK

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout del repo
      - uses: actions/checkout@v4

      # 2️⃣ Instalar JDK 17
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # 3️⃣ Instalar Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'

      # 4️⃣ Instalar dependencias npm
      - name: Install Dependencies
        run: npm ci

      # 5️⃣ Construir web app
      - name: Build Web App
        run: npm run build

      # 6️⃣ Eliminar carpeta Android vieja
      - name: Remove old Android folder
        run: |
          if [ -d "android" ]; then
            rm -rf android
          fi

      # 7️⃣ Crear Android limpio y sincronizar
      - name: Add Android with Capacitor
        run: |
          npx cap add android
          npx cap sync android

      # 8️⃣ Construir APK
      - name: Build APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon --no-build-cache

      # 9️⃣ Subir APK como artifact
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
